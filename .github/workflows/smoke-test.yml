name: Smoke Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  PYTEST_TIMEOUT_ARGS: >-
    --pytest-args "--timeout=600"
  # Scala JVMs are slower to start on 2-core/7GB GitHub runners
  SCALA_PYTEST_TIMEOUT_ARGS: >-
    --pytest-args "--startup-timeout=600" --pytest-args "--timeout=600" --pytest-args "--timeout-scale=1.5"
  FINALIZED_BLOCK_TARGET: 10

jobs:
  validate:
    name: Validate & CLI
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install shardctl
        run: |
          pip install poetry
          poetry install
      - name: Validate all compose files
        run: |
          FAIL=0
          for cf in \
            compose/f1r3node-rust.yml compose/f1r3node-rust-standalone.yml \
            compose/f1r3node-rust-observer.yml compose/f1r3node-rust-validator4.yml \
            compose/f1r3node.yml compose/f1r3node-standalone.yml \
            compose/f1r3node-observer.yml compose/f1r3node-validator4.yml \
            compose/f1r3node-shard-light.yml compose/embers.yml \
            compose/f1r3sky.yml compose/monitoring.yml \
            integration-tests/docker-compose.rust.yml integration-tests/docker-compose.scala.yml \
            integration-tests/docker-compose.standalone-rust.yml integration-tests/docker-compose.standalone-scala.yml
          do
            if docker compose --env-file .env.node -f "$cf" config >/dev/null 2>&1; then
              echo "PASS: $cf"
            else
              echo "FAIL: $cf"
              FAIL=$((FAIL + 1))
            fi
          done
          [ "$FAIL" -eq 0 ] || (echo "$FAIL compose file(s) failed validation" && exit 1)
      - name: Verify shardctl CLI
        run: |
          poetry run shardctl --help
          poetry run shardctl status || true
          poetry run shardctl ps || true

  # ===========================================================
  # Topology Health Checks (10 finalized blocks each)
  # ===========================================================
  rust-shard:
    name: Rust Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Start shard and wait for Running
        run: |
          poetry run shardctl up f1r3node-rust
          poetry run shardctl wait --timeout 300
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 60); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockInfo',{}).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 300s" && exit 1)
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-rust-shard
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  rust-standalone:
    name: Rust Standalone (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Start standalone and wait for Running
        run: |
          poetry run shardctl up f1r3node-rust-standalone
          poetry run shardctl wait --timeout 120
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 60); do
            LFB=$(curl -sf http://localhost:40403/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockInfo',{}).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 300s" && exit 1)
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-rust-standalone
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-shard:
    name: Scala Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start shard and wait for Running
        run: |
          poetry run shardctl up f1r3node
          poetry run shardctl wait --timeout 420
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 60); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockInfo',{}).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 300s" && exit 1)
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-shard
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-standalone:
    name: Scala Standalone (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start standalone and wait for Running
        run: |
          poetry run shardctl up f1r3node-standalone
          poetry run shardctl wait --timeout 120
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 60); do
            LFB=$(curl -sf http://localhost:40403/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockInfo',{}).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 300s" && exit 1)
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-standalone
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-light:
    name: Scala Light Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start light shard and wait for Running
        run: |
          poetry run shardctl up f1r3node-shard-light
          poetry run shardctl wait --timeout 300
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 60); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockInfo',{}).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 300s" && exit 1)
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-light
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  # ===========================================================
  # Integration Tests (each on its own runner)
  # ===========================================================
  rust-test-shard:
    name: "Rust: test_web_api (shard)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust test_web_api ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-rust-test-shard
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  rust-test-standalone:
    name: "Rust: test_heartbeat (standalone)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust "test_heartbeat_creates_blocks_when_idle and not shard" ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-rust-test-standalone
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  rust-test-custom:
    name: "Rust: test_synchrony (custom)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust test_synchrony_constraint ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-rust-test-custom
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-shard:
    name: "Scala: test_web_api (shard)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala test_web_api ${{ env.SCALA_PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-test-shard
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-standalone:
    name: "Scala: test_heartbeat (standalone)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala "test_heartbeat_creates_blocks_when_idle and not shard" ${{ env.SCALA_PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-test-standalone
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-custom:
    name: "Scala: test_synchrony (custom)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala test_synchrony_constraint ${{ env.SCALA_PYTEST_TIMEOUT_ARGS }}
      - name: Collect logs
        if: always()
        run: |
          mkdir -p /tmp/logs
          docker ps -a > /tmp/logs/containers.txt 2>&1 || true
          for c in $(docker ps -aq --filter "name=rnode" 2>/dev/null); do
            name=$(docker inspect --format '{{.Name}}' "$c" | sed 's|^/||')
            docker logs "$c" > "/tmp/logs/${name}.log" 2>&1 || true
          done
          cp integration-tests/report.json /tmp/logs/ 2>/dev/null || true
      - uses: actions/upload-artifact@v4
        if: always()
        with:
          name: logs-scala-test-custom
          path: /tmp/logs/
          retention-days: 7
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

# =============================================================
# Not yet covered:
# - shardctl clone / setup / build-service (requires source builds, ~30min+)
# - shardctl exec / shell (interactive)
# - Embers, F1R3Sky, monitoring topologies (separate service images)
#
# TODO: Once rust-client has a published binary or fast compile:
#   - Test shardctl clone to pull service repos
#   - Build rust-client via shardctl build-service
#   - Use rust-client to verify LFB advancement (deploy + propose + finalize)
#   - This would replace the curl-based LFB polling with a proper e2e check
# =============================================================
