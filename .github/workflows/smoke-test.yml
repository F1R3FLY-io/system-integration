name: Smoke Test

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:

env:
  PYTEST_TIMEOUT_ARGS: >-
    --pytest-args "--timeout=600"
  FINALIZED_BLOCK_TARGET: 10

jobs:
  validate:
    name: Validate & CLI
    runs-on: ubuntu-latest
    timeout-minutes: 5
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - name: Install shardctl
        run: |
          pip install poetry
          poetry install
      - name: Validate all compose files
        run: ./scripts/smoke-test.sh --validate-only
      - name: Verify shardctl CLI
        run: |
          poetry run shardctl --help
          poetry run shardctl status || true
          poetry run shardctl ps || true

  # ===========================================================
  # Topology Health Checks (25 blocks each)
  # ===========================================================
  rust-shard:
    name: Rust Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Start shard and wait for Running
        run: |
          poetry run shardctl up f1r3node-rust
          poetry run shardctl wait --timeout 300
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 120); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 600s" && exit 1)
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  rust-standalone:
    name: Rust Standalone (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Start standalone and wait for Running
        run: |
          poetry run shardctl up f1r3node-rust-standalone
          poetry run shardctl wait --timeout 120
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 120); do
            LFB=$(curl -sf http://localhost:40403/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 600s" && exit 1)
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-shard:
    name: Scala Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start shard and wait for Running
        run: |
          poetry run shardctl up f1r3node
          poetry run shardctl wait --timeout 420
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 120); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 600s" && exit 1)
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-standalone:
    name: Scala Standalone (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 15
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start standalone and wait for Running
        run: |
          poetry run shardctl up f1r3node-standalone
          poetry run shardctl wait --timeout 120
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 120); do
            LFB=$(curl -sf http://localhost:40403/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 600s" && exit 1)
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  scala-light:
    name: Scala Light Shard (10 finalized)
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Start light shard and wait for Running
        run: |
          poetry run shardctl up f1r3node-shard-light
          poetry run shardctl wait --timeout 300
          poetry run shardctl status
      - name: Wait for 10 finalized blocks
        run: |
          TARGET=${{ env.FINALIZED_BLOCK_TARGET }}
          for i in $(seq 1 120); do
            LFB=$(curl -sf http://localhost:40413/api/last-finalized-block 2>/dev/null | python3 -c "import sys,json; print(json.load(sys.stdin).get('blockNumber',0))" 2>/dev/null || echo 0)
            echo "  [$((i*5))s] LFB: #$LFB"
            if [ "$LFB" -ge "$TARGET" ] 2>/dev/null; then
              echo "Reached LFB #$LFB"
              break
            fi
            sleep 5
          done
          [ "$LFB" -ge "$TARGET" ] 2>/dev/null || (echo "FAIL: LFB only at #$LFB after 600s" && exit 1)
      - name: Cleanup
        if: always()
        run: poetry run shardctl reset -y || true

  # ===========================================================
  # Integration Tests (each on its own runner)
  # ===========================================================
  rust-test-shard:
    name: "Rust: test_web_api (shard)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust test_web_api ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  rust-test-standalone:
    name: "Rust: test_heartbeat (standalone)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust test_heartbeat_creates_block ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  rust-test-custom:
    name: "Rust: test_synchrony (custom)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 20
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-rust-node:latest
      - name: Run test
        run: poetry run shardctl test --rust test_synchrony_constraint ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-shard:
    name: "Scala: test_web_api (shard)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala test_web_api ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-standalone:
    name: "Scala: test_heartbeat (standalone)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala test_heartbeat_creates_block ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

  scala-test-custom:
    name: "Scala: test_synchrony (custom)"
    runs-on: ubuntu-latest
    needs: validate
    timeout-minutes: 25
    steps:
      - uses: actions/checkout@v5
      - uses: actions/setup-python@v5
        with:
          python-version: "3.10"
      - run: |
          pip install poetry
          poetry install --with integration
      - run: docker pull f1r3flyindustries/f1r3fly-scala-node:latest
      - name: Run test
        run: poetry run shardctl test --scala test_synchrony_constraint ${{ env.PYTEST_TIMEOUT_ARGS }}
      - name: Cleanup
        if: always()
        run: |
          poetry run shardctl reset -y || true
          poetry run shardctl test-reset || true

# =============================================================
# Not yet covered:
# - shardctl clone / setup / build-service (requires source builds, ~30min+)
# - shardctl exec / shell (interactive)
# - Embers, F1R3Sky, monitoring topologies (separate service images)
# =============================================================
